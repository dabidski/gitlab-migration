source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'gitlab', :git => 'git@github.com:dabidski/gitlab.git'

# Specify your gem's dependencies in gitlab_migration.gemspec
gemspec
